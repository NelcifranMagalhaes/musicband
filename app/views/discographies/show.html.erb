&nbsp;
<p></p>
<% if notice.present? %>
<div class="notification">
	<p id="notice"><%= notice %></p>
</div>
<% end %>
<div class="container">
	<div class="tile is-ancestor">
		<div class="tile is-4 is-vertical is-parent">
			<div class="tile is-child box">
				<p class="title">Nome</p>
				<p><%= @discography.name %></p>
			</div>
			<div class="tile is-child box">
				<p class="title">Data de Lançamento</p>
				<p><%=l @discography.date_rise %></p>
			</div>
		</div>
		<div class="tile is-parent">
			<div class="tile is-child box">
				<p class="title">Letra</p>
					<%= link_to @discography.description do%>
						<span style="font-size: 48px; color: red;">
	                		<i class="fa fa-youtube fa-3x"></i>
						</span>
					<% end %>
			</div>
		</div>
	</div>
	<div class="buttons">
		<% if current_user.present? %>
			<%= link_to 'Editar', edit_discography_path(@discography) ,class: "button is-link" %>
		<% end %>
		<%= link_to 'Voltar', root_path ,class: "button" %>
	</div>
</div>


